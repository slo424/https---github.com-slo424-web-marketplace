import React, { FC } from 'react';

interface PlayGroundComponentProps {}

const PlayGroundComponent: FC<PlayGroundComponentProps> = (props: any) => (
   // const navigate = useNavigate();
   // return (
     <div>
       <h2>Test</h2>
   </div>
   // );
);

export default PlayGroundComponent;
